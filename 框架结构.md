# CATIA V5R21 CAA 解析框架结构

本文件只描述当前新仓库中的 `D:\3Djiexi\3DjiexiCAA`，旧工程不在维护范围内。

## 目录树

```text
3DjiexiCAA/
├─ CadParseMvp.edu/
│  ├─ IdentityCard/
│  │  └─ IdentityCard.h
│  └─ CadParseMvp.m/
│     ├─ Imakefile.mk
│     └─ src/
│        ├─ CadParseContracts.h
│        ├─ CadParseCore.cpp
│        ├─ CadParseIR.h
│        ├─ CadParseIR.cpp
│        ├─ CadParseCAA.h
│        ├─ CadParseCAA.cpp
│        ├─ CadParseBatch.cpp
│        └─ CadParseSelfTests.cpp
├─ tests/
│  └─ CadParseCoreTestMain.cpp
├─ tools/
│  ├─ build_r21_x86.bat
│  ├─ run_r21_x86.bat
│  └─ test_core_vs2008.bat
└─ docs/
   ├─ CAA_MVP_ARCHITECTURE.md
   ├─ CAA_R21_API_EVIDENCE.md
   ├─ CAA_MVP_BUILD_AND_RUN.md
   ├─ CAA_DECODER_EXTENSION_GUIDE.md
   ├─ CAA_NATIVE_HOLE_DECODER.md
   ├─ CAA_PARAMETER_DECODER.md
   ├─ CAA_DECLARED_BUSINESS_FEATURE.md
   └─ CATIA_R21_NATIVE_HOLE_FIXTURES.md
```

## 每个 C++/头文件一句话说明与调用关系

| 文件 | 一句话用途 | 主要调用关系 |
|---|---|---|
| `CadParseContracts.h` | 定义不依赖 CATIA 的 Feature、参数、Native Hole Typed Payload、统计、诊断、Native View 和 Decoder 公共契约。 | 被 Core、IR、CAA、Batch、SelfTests 全部引用，是最底层接口。 |
| `CadParseCore.cpp` | 实现 Registry、String/Hole Decoder、Generic/Opaque 兜底、参数规范化/归属、业务聚合、Hole/对象守恒、SHA-256 和路径脱敏。 | 接收 CAA Native View，按注册机制选择 Decoder，向 IR Writer 提供纯数据记录。 |
| `CadParseIR.h` | 声明事务式 `JsonArtifactWriter` 的完整写出接口和简化测试接口。 | Batch/Core 测试调用，依赖 Contracts。 |
| `CadParseIR.cpp` | 一次生成四个核心 JSONL、诊断、Coverage、Manifest 和日志，并在校验后原子提交输出目录。 | 消费 Core/Crawler 产生的纯数据，不调用 CAA API。 |
| `CadParseCAA.h` | 声明 Session/Document RAII、UniversalFeatureCrawler、Decoder 工厂和 Unicode 转换边界。 | Batch 调用；实现细节位于 CadParseCAA.cpp。 |
| `CadParseCAA.cpp` | 使用 R21 PublicInterfaces 打开/遍历 CATPart，读取真实 String 参数，并从当前 `CATISpecObject` 查询 `CATIAHole` 读取原生 Hole 设计参数。 | 把 CATIA 对象适配成 Contracts 视图，RAII 释放接口，调用 Registry，产生 Feature/关系。 |
| `CadParseBatch.cpp` | 解析命令行，串联哈希、Session、Document、Crawler、参数索引、业务聚合、校验和一次事务写出。 | 程序入口，依次调用 CAA → Core → IR。 |
| `CadParseSelfTests.cpp` | 用 Fake View/Decoder/Object Tree 测试 String/Hole 匹配、异常隔离、Typed Payload、JSON 类型、守恒、事务和 Golden 输出。 | 由独立测试 main 或 Batch `--self-test` 调用，完全不引用 CATIA 头文件。 |
| `CadParseCoreTestMain.cpp` | 提供无需 CATIA 许可证的最小测试进程入口。 | 只创建并运行 `SelfTestSuite`。 |

## 关键调用链

```text
main / RunBatch
  → SessionGuard
  → DocumentGuard
  → UniversalFeatureCrawler
      → SpecObjectView
          → CATICkeParm / CATICkeType / CATICkeInst
          → QueryInterface(IID_CATIAHole)
              → CATIAHole / CATIALimit / CATIALength / CATIAStrParam
      → FeatureTypeRegistry
          → KnowledgewareStringParameterDecoder
          → NativeHoleDecoder
          → 基础 Typed Decoder
          → GenericFeatureDecoder
          → OpaqueObjectRecorder
  → ParameterRecordBuilder
      → ParameterOwnershipResolver
  → DeclaredBusinessFeatureAggregator
      → BusinessFeatureRuleCatalog
  → JsonArtifactWriter
      → 守恒和引用完整性校验
      → staging 写出
      → SHA-256 / Manifest
      → 原子目录提交
```

## 输出关系

- `features.jsonl`：所有 CAA 枚举对象；String 参数和 Native Hole Typed Payload 都附着在各自原始 Feature 上参与对象守恒。
- `relations.jsonl`：当前已验证的 `parent_of` 和 `contains`。
- `parameters.jsonl`：复用 Feature ID 的参数消费索引，不创建第二套对象 ID。
- `business_features.jsonl`：GSMTool 声明树聚合结果，不属于 B-Rep 或原生 Hole/Pad 识别。
- `coverage.json`：原始对象、参数、业务特征和 Native Hole 候选四套守恒及探测/stale 汇总。
- `manifest.json`：版本、Git、输入大小/SHA-256、运行时和源文件提示、产物哈希。

## Feature Center V1 Sidecar 与 Viewer

```text
backend/app/feature_center/
├─ contracts.py          # Observation、Canonical Feature、Measurement 和 Bundle 契约
├─ step_input.py         # STEP Header、Schema、单位、哈希与坐标变换检查
├─ topology.py           # Shape Hash、尺度容差和稳定拓扑编号
├─ eaag.py               # 面邻接、共享边、Wire 与引用查询
├─ hole_mapping.py       # Native Hole 参数引导的真实 Face 验证
├─ fusion.py             # CAA/B-Rep Observation 和 Canonical Feature 融合
├─ mesh.py               # 一面一 Primitive 的 GLB 与双向映射
├─ readiness.py          # Rib/Web、Cavity/Island、Freeform 就绪度探针
├─ visual_review.py      # 禁用远程调用的离线视觉审查协议
├─ bundle.py             # 确定性 JSONL、哈希校验和事务式发布
└─ service.py            # Sidecar 各阶段编排

frontend/src/views/feature-center/
├─ index.vue                         # 特征列表、真实面高亮、反查、透明/隔离/剖切
└─ modules/feature-center-bundle.ts # Bundle JSONL 读取与 Feature/Face 索引
```

调用链为：`feature_center.py → FreeCAD 子进程 → topology/eAAG → HoleGeometryVerifier → fusion → mesh → bundle → Feature Center Viewer`。复杂结构探针不会创建生产 Canonical Feature，视觉审查当前也不会发起网络请求。
