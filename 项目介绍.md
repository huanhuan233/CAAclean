# 3D 解析与专利标注项目介绍

> 本文档面向后续接手项目的人类开发者与 AI 协作者，说明项目目标、代码边界、运行方式和当前状态。开始修改前，请先阅读本文与 `docs/` 下对应的设计/实施文档。

## 1. 项目目标

本项目是一套面向机械零部件的辅助解析与标注系统，主要覆盖两条业务链路：

1. **STEP/CAD 三维解析**：上传 STEP/STP 文件，调用 FreeCAD 无界面解析，保存模型版本、实体层级、拓扑、网格、特征候选和测量结果。
2. **工程图与专利图纸标注**：导入图纸或 PDF，提取结构化信息；支持人工绘制/编辑引线标注，并提供基于 PDF 文本解析和视觉模型定位的专利附图自动标注辅助能力。

系统定位是“可审阅的辅助工具”：自动识别和定位结果应保留置信度、来源和审阅状态，不能静默覆盖人工结果。

## 2. 技术架构

```text
Vue 3 + TypeScript 前端（frontend/）
        │ HTTP / multipart
        ▼
FastAPI 后端（backend/）
 ├─ CAD：FreeCAD 子进程解析 STEP
 ├─ 工程图：图纸布局/事实/规格字段处理
 ├─ 部件构建：汇聚图纸、CAD、人工来源
 └─ 专利标注：PDF 文本解析 + 可选视觉模型定位
        │
        ├─ PostgreSQL（业务数据与解析结果）
        ├─ FreeCAD（服务器端 headless 运行）
        ├─ MinerU（可选，PDF 结构化文本解析）
        └─ 视觉模型服务（可选，专利图中部件定位）
```

## 3. 代码与目录职责

| 路径 | 职责 |
| --- | --- |
| `backend/` | FastAPI 服务、数据库模型、解析与业务逻辑、pytest 测试。 |
| `backend/app/cad/` | STEP 上传、版本状态、FreeCAD 调用、实体树、拓扑、测量和特征接口。 |
| `backend/app/drawing/` | 工程图导入、布局、标注区域、事实提取与规格字段相关能力。 |
| `backend/app/component_builds/` | 把 CAD、图纸和人工信息汇聚为部件构建数据。 |
| `backend/app/patent_annotation/` | 专利说明书解析、图纸部件定位及其 API。 |
| `backend/app/core/` | 配置、MinerU 客户端、视觉模型客户端等共享基础设施。 |
| `backend/freecad_scripts/` | 由 FreeCAD 执行的解析脚本。 |
| `backend/tests/` | 后端单元与接口测试。 |
| `frontend/` | Vue 3/Vite/TypeScript 管理端（基于 SoybeanAdmin Element Plus）。 |
| `frontend/src/views/cad-model/` | CAD 模型与三维解析页面。 |
| `frontend/src/views/cad-spec/` | 工程图与规格相关页面。 |
| `frontend/src/views/component-build/` | 部件构建/数据融合页面。 |
| `frontend/src/views/patent-annotation/` | 专利 PDF 标注工作台、引线编辑与自动标注界面。 |
| `docs/acceptance/` | 分阶段验收记录。 |
| `docs/superpowers/specs/` | 已确认的设计说明；改动相关模块前优先阅读。 |
| `docs/superpowers/plans/` | 对应设计的实施计划与历史上下文。 |
| `cad-work/`、`cad-spec-work/` | 运行时工作目录，通常不应提交业务生成物。 |

## 4. 关键业务流程

### 4.1 STEP/CAD 解析

1. 前端上传 `.step` / `.stp` 文件到 `/api/cad`。
2. 后端创建模型和解析版本记录。
3. 后端以独立子进程调用 `backend/freecad_scripts/parse_step.py`。
4. FreeCAD 返回实体、父子关系、拓扑和网格等 JSON 结果；后端校验并写入 PostgreSQL。
5. 前端按版本状态读取模型树、实体、面/边拓扑、测量和特征结果。

FreeCAD 只应负责几何解析；它不直接连接数据库，也不应把业务规则写进脚本。

### 4.2 图纸、规格与部件构建

工程图信息与 FreeCAD 测量值会进入独立的事实、规格字段和证据结构。图纸标称值是主要依据；几何测量用于比对和提供证据，不能反向覆盖图纸值。`component_builds` 模块在此基础上融合 CAD、图纸、人工输入等来源，形成可继续编辑和审阅的部件数据。

### 4.3 专利 PDF 自动标注

1. 用户在专利标注页面上传说明书 PDF，并准备需要标注的无引线附图页面。
2. 后端优先通过 MinerU 解析文本；不可用或失败时回退到 `pypdf`。
3. 系统提取部件编号、名称、附图说明、图号上下文与局部放大标记。
4. 前端将当前 PDF 页渲染为 PNG；后端可调用视觉模型服务返回候选部件位置与置信度。
5. 前端对引线锚点进行吸附和排布，生成可编辑标注。
6. 自动结果仅替换当前页的旧自动结果，必须保留人工标注；低置信度结果需要人工复核。

详细约束见 `docs/superpowers/specs/2026-07-24-patent-pdf-auto-annotation-mineru-design.md`。

## 5. 本地运行

### 后端

建议使用名为 `3dcad` 的 Conda 环境：

```powershell
conda activate 3dcad
python -m pip install -r backend/requirements.txt
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 5180 --reload
```

测试：

```powershell
conda run -n 3dcad python -m pytest backend/tests -v
```

真实 STEP 解析需要本机或服务器能够运行 FreeCAD。关键环境变量包括：

```text
FREECAD_CMD             # freecadcmd 可执行文件路径
FREECAD_TIMEOUT         # 解析超时秒数
CAD_SCRIPT_DIR          # FreeCAD 脚本目录
CAD_WORK_DIR            # CAD 运行工作目录
DATABASE_URL            # PostgreSQL 连接串；也支持拆分的 POSTGRES_* 配置
```

服务器无图形环境时，通常还需设置 `QT_QPA_PLATFORM=offscreen` 与 `LIBGL_ALWAYS_SOFTWARE=1`。不要把真实密码、API Key 或服务器路径写入源码或提交到 Git。

### 前端

前端要求 Node.js `>=20.19.0`，使用 pnpm：

```powershell
cd frontend
pnpm install
pnpm dev
```

常用检查：

```powershell
pnpm typecheck
pnpm test:patent-annotation
pnpm build
```

## 6. 接口入口

后端在 `backend/app/main.py` 注册以下模块：

- `/api/health`：服务与数据库健康检查。
- `/api/cad`：CAD 上传、解析状态、模型树、实体、拓扑、测量、特征等。
- `/api/drawing`：工程图处理。
- `/api/component-builds`：部件构建及数据融合。
- `/api/patent-annotations`：专利文档解析与单页定位。

启动服务后可通过 FastAPI 自动文档确认精确接口和请求结构。

## 7. 当前状态与接手注意事项

- 项目已具备 CAD 解析、工程图/规格数据、部件构建和专利标注的主要代码结构；具体完成度以 `docs/acceptance/` 和对应测试为准。
- 最近提交集中在专利 PDF 自动标注与手动标注页面。接手该模块请先阅读其设计说明和 `frontend/src/views/patent-annotation/` 下的测试。
- 当前工作区存在**尚未提交的改动和运行产物**，涉及专利标注前后端文件、测试、`.pytest_tmp/`、`.runtime/`、`cad-spec-work/` 等。接手前先执行 `git status --short`，不要把这些改动当作可随意清理的临时文件。
- 根目录 `.env` 可能含本机配置，不能在交流、截图或提交中泄露其中的凭据。
- Python 依赖中不包含完整 FreeCAD 运行时；真实三维解析依赖外部 FreeCAD 安装及环境变量配置。
- MinerU 和视觉模型是可选外部能力。PDF 文本解析在 MinerU 不可用时会回退；视觉定位未配置时应向用户清楚报告，而不是伪造自动标注结果。

## 8. 推荐接手顺序

1. 阅读本文和 `git status --short`，确认未提交改动的归属。
2. 分别启动后端与前端，先跑相关单元测试，再进行功能调试。
3. 按任务阅读对应的 `docs/superpowers/specs/` 与 `docs/superpowers/plans/`，避免凭页面现状猜测业务语义。
4. 对自动识别、几何测量、模型定位等不确定结果，始终保留来源、置信度和人工审阅入口。
5. 修改接口时同步更新前端 `src/service/api/`、`src/typings/api/` 与相关测试。

## 9. 进一步资料

- 后端安装、FreeCAD 与数据库配置：`backend/README.md`
- 各阶段验收：`docs/acceptance/`
- CAD 最小后端实施背景：`docs/superpowers/plans/2026-07-21-cad-backend-minimal.md`
- 专利自动标注设计：`docs/superpowers/specs/2026-07-24-patent-pdf-auto-annotation-mineru-design.md`

